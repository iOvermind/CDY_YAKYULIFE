# 建置的 context 是專案根目錄，少了這一份會把 node_modules 與整個 .git 都送給
# Docker daemon——那是幾百 MB 的無謂搬運，而且 node_modules 還會被 Dockerfile
# 裡的 npm ci 覆蓋掉。

.git
.github
node_modules
**/node_modules

# 前端在 image 裡重新建置，本機的產物不要帶進去（架構還可能不一樣）。
client/dist

# 密碼與金鑰是執行時的環境變數，絕不烤進 image。
**/.env
!**/.env.example

# 只有本機開發與測試會用到的東西。dev.ts 跑的是記憶體假資料庫，
# 不該出現在正式的 image 裡。
#
# **devdb.ts 也要排除**：它 import fakedb.ts，而 fakedb 已經被排掉了——留著它等於
# 在 image 裡放一個 import 不存在檔案的模組。正式路徑沒有人載入它，所以不會炸，
# 但那是一顆等著被誰 import 一下就爆的地雷。
server/src/dev.ts
server/src/devdb.ts
server/src/fakedb.ts
**/*.test.ts

# 文件與 legacy 參考檔，執行時用不到。
docs
*.md
index_legacy.html

# 但 CHANGELOG.md 是前端建置的輸入：client/scripts/changelog.mjs 會把它轉成
# client/src/data/changelog.json。少了它 prebuild 會直接 ENOENT 停在那裡。
!CHANGELOG.md
