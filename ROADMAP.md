# 專案開發藍圖 (Roadmap)

本文件記錄《棒球人生模擬器 2.0》的重大重構計畫與待辦清單 (To-Do List)。我們正從單一靜態 HTML 轉型為 **Tauri + React + 本地/雲端雙軌資料庫** 的現代化應用程式。

## 階段一：架構重構與本地儲存 (已啟動)

*   [x] **資料解耦**：將硬編碼的天賦、事件、常數抽出為 `client/src/data/` 底下的 10 個 JSON（`abilities` / `leagues` / `positions` / `teams` / `events` / `traits` / `awards` / `hall_of_fame` / `amateur` / `flavor`）。
*   [x] **Tauri + React 專案初始化**：建立 `client/` 目錄與 Tauri v2 + React 19 + Vite 7 的工具鏈。
*   [ ] **UI 元件化**：將 `index_legacy.html` 的畫面拆分為 React Components 並移植樣式。`client/src/App.tsx` 已有開始畫面、事件卡日誌、狀態欄、能力表、成績表與訓練骰列；尚未拆成獨立元件，主題與周邊視覺也還沒動。
*   [x] **確立防護與資料架構 (ADR 0001)**：制定「網頁端連線 PG，桌面端離線 SQLite」的兩棲存取策略，確立 Progression Isolation 機制。
*   [ ] **實作 SQLite / IndexedDB 介面卡**：讓 React 前端能依據執行環境（Browser/Tauri）切換存取介面。
*   [ ] **開發 `SimulationEngine.ts`**：將原本寫在 `index_legacy.html` 的擲骰、升降級與結算邏輯移植至 TypeScript，全面擁抱物件導向與資料驅動 (Data-Driven)。

## 階段一.五：功能對等 (Feature Parity)

> **這個階段的唯一目標**：新架構能從高中開局、完整走完一段生涯到引退與名人堂結算，中途不卡關。
>
> **驗收線**：跑得完一段生涯所需的全部系統。外觀與周邊（四種主題、分享圖產生）不在此階段——介面本來就要改成科技藍主題，搬舊的會白做。
>
> **不是逐數值對等，也不是模型對等**：對等指的是**系統齊全、流程走得完**。有兩處已刻意偏離 legacy——守備分權重的修正（見 `client/src/data/positions.json` 的 `_deviation`），以及下方的二刀流球員模型。新舊版的數字本來就不會相同。

### 球員模型（已偏離 legacy，刻意）

*   [x] **全能力模型**：每位球員都擁有全部 15 項能力，不再有投手／野手的能力二分。開局選的**起始守位**只影響潛力天賦的機率加權與初始能力加成，不決定你有哪些能力。見 `client/src/data/abilities.json` 的 `_model_note`。
*   [x] **四大球系取代 brk**：`swp` / `drp` / `chg` / `gim`，見 `docs/design/simulation_math.md` §5。
*   [x] **投打慣用手**：§5 的同邊優勢與反邊剋星需要投打雙方的慣用手才算得出來。左右開投列為隱藏特性，不進開局擲骰。
*   [x] **二刀流天賦**：高中畢業／選秀前判定，見「養成期」一節。取得後投打兩側的成長曲線更平緩，兩邊都點得起來。
*   [ ] **二刀流的成績與評價**：投手成績與野手成績分開累積、分開顯示、分開評獎——單位不同，硬湊沒有意義。但 WS / WAR 這類把不同貢獻換算成同一尺度的通用指標可以跨投打加總，另闢一頁「合併生涯紀錄」，讓二刀流能與單一領域的球員同榜比較。見 `client/src/data/hall_of_fame.json` 的 `two_way`。

### 底層（必須最先做）

*   [x] **可重現亂數（種子碼）**：`client/src/engine/rng.ts`，見 [ADR 0002](docs/adr/0002-deterministic-rng-and-replay-log.md)。主種子衍生 `genesis` / `growth` / `events` / `health` / `season` / `career` 六條子序列，同版本內完全確定、跨版本不保證。
*   [x] **卡片與選項的流程編排**：`client/src/engine/flow.ts`。對應 `index_legacy.html` 的 `stepQ`，但改為**選擇驅動的狀態機**——引擎自行推進，跑到需要玩家決定的地方停下，選擇是唯一的外部輸入。舊版用 callback 串接，流程狀態散在閉包鏈裡，無法支撐重播。
*   [x] **重播機制（選擇序列）**：`client/src/engine/game.ts` 的 `toReplayLog()` / `Game.replay()`。存「種子＋選擇序列」而非狀態快照，重播即可還原。用於意外中斷後的回復，以及 ADR 0001 要的伺服器端重跑驗證。**不是存檔功能**，也無法用來重刷結果。帶引擎版本號，跨版本直接拒絕。
*   [ ] **遊戲狀態模型**：已有球員能力、蓄力槽、年齡年份、榮譽、待分配點數、賽季成績、合約、守位登錄、傷病史與感情；尚缺 TJ 量表。
*   [ ] **種子碼說明的措辭修正**：ADR 0002 決定跨版本不保證重現，因此 `README.md` 與遊戲內的種子碼提示（`index_legacy.html:334` 的「相同種子＋相同選擇＝相同人生」）**必須**補上版本限制，否則是對玩家的錯誤承諾。
*   [ ] **生涯結果的保存**：引退結算後寫入紀錄，供天梯與成就點數使用。與重播日誌用途不同，不可混為一談。

### 養成期

*   [x] **高中大賽結算與能力點分配**：`client/src/engine/amateur.ts`。大學與業餘成棒的資料已備好（`amateur.json` 的 `cups`），只差流程接上。
*   [x] **綜合能力評價**：`client/src/engine/rating.ts`。投手側與野手側各算一個評價，整體取較高者。
*   [x] **年度循環**：養成六年（國中三年 + 高中三年）可完整跑完，年齡與年份逐年推進，升學時重新分發學校。
*   [x] **國中養成階段**：生涯起點前移至 13 歲。這不是內容擴充而是平衡的地基——三年制下畢業綜合僅約 38，選秀第 1 輪與大賽冠軍都構不到；六年制拉到約 46。
*   [x] **養成期國際賽**：國中 U-15、高中 U-18，各在該階段第 3 年舉辦，需通過徵召門檻才入選。
*   [x] **每年的事件卡張數**：國中 1 張／高中 2 張／職業 3 張，寫在 `events.json` 的 `cards_per_year`。legacy 是 `nEv = stage==='PRO' ? 3 : 2`（它沒有國中階段），國中那一階是新架構才拆出來的——十三歲的一年裡不會發生那麼多事。
*   [x] **事件卡**（保守／照常／全力一搏）：`client/src/engine/events.ts`。效果幅度改為「效果值 × 應對倍率」，舊版只取正負號、幅度全由應對決定，稀有事件因此無法比常見事件更重。
*   [x] **能力上限提升**：少數事件可提升某項能力的上限（每項最多 +5，因此被提升過的能力最高可練到 85）。提升上限**不直接加能力值**，點數仍要自己練上去。
*   [x] **訓練骰與蓄力槽**：`client/src/engine/growth.ts`。成本曲線統一為一條（`<50` 1 點／`50-57` 2／`58-63` 4／`64-71` 6／`≥72` 8，超過天賦 ×3）；二刀流不另立曲線，改為直接扣點（天賦內 −1、天賦外 −3）。
*   [x] **配點的復原與確認**：一次提問管到底，可逐點分配、隨時復原，全部分完才能確認。復原本身也寫進重播日誌——配點不消耗亂數，反向操作是精確的。
*   [x] **選秀**：`client/src/engine/draft.ts`。球團評價 = 綜合能力 + 年齡加權 + 臨場波動；後段輪次可拒絕指名再拚一年（年齡未達上限時）。
*   [x] **二刀流天賦判定**：高中畢業、選秀之前判定。投手側與野手側都達門檻（42）才算——單邊很強只代表專精。實測專精者取得率 0%、刻意兩邊練的人約 34%，因此它是一個 build 選擇而非抽獎。
*   [x] **定位鎖定**：養成結束沒取得二刀流就要選邊站，另一側的能力表整組收起、不再能加點，投打定位也不再隨評價互換。這是二刀流的代價面。
*   [x] **養成期成績**：投打一律都記，不看能力偏向——國高中的球隊人數有限，投手排進打線是常態。

### 職業期

> 目前只跑 CPBL 主軸（二軍 ↔ 一軍）。`leagues.json` 已備妥 NPB／MLB／KBO／LMB／ABL
> 的階梯資料，但轉會與尋路尚未實作。

*   [x] **聯盟階梯升降級**：`client/src/engine/pro.ts`。判定看能力相對該層級的 par／min，不看單季成績——成績本身就是能力加噪音，用它判定等於把同一份噪音算兩次。判定順序為升級 → 降級 → 戰力外，戰力外留兩年寬限期。
*   [x] **賽季模擬與成績結算**：`client/src/engine/season.ts`，照 `simulation_math.md` §3-4 的「先算時間，再算機率」。
*   [x] **年齡曲線與引退**：26 歲前緩升、26–30 巔峰、之後遞減且隨年齡加速；速度與守備範圍先掉，接觸與選球撐得久。
*   [x] **球隊戰力與奪冠機率**：`client/src/engine/teams.ts`。取代舊版那個「把隊名雜湊成 8–29%」的假數字。
*   [x] **進階指標**：ERA+／OPS+／Win Shares，`client/src/engine/metrics.ts`。WS 是近似值，見 `docs/design/formulas.md`。
*   [x] **守位系統與移防**：`client/src/engine/defense.ts`。進入頂級聯盟時登錄守位，之後每年重新檢視；移防雙向，守備練回來就移得回去。守備分與**同守位的平均**比較，守位價值由責任占比表達（捕手最重、一壘最輕）。
*   [x] **聯盟水準逐年浮動**：`client/src/engine/league.ts`。par 與 min 在基準上下擺盪，**以體系為單位**——一軍與二軍同進同退。兩者的差距本身也會擺盪，因此評價分的替代水準逐年不同。走 `career` 子序列（與球隊戰力同類，都是生涯層級的世界逐年變動），重播可還原。
*   [x] **逐段（stint）成績紀錄**：`SeasonRecord`。份額與 k 在球季當下就算好存進去，不留到結算時回算——那些值取決於當年的聯盟水準。季中下放與季中轉隊的**紀錄形狀**已經做對（一年可以有多段），但季中異動本身還沒實作；呈現要沿用 legacy 的「舊隊／新隊／合計」三行。
*   [x] **合約系統**：`client/src/engine/contract.ts`。年限（成績＋年齡＋傷病）、長約／短約取捨、掌控期五年、延長續約、到期處理、買斷。傷病與否決交易的輸入**公式已寫完整但恆為 0**，等對應系統上線只要接數字。
*   [x] **FA 市場**：跳出合約測試市場、乏人問津時的減薪回歸與引退選項。報價來自尋路但**不列降級選項**——FA 問的是「誰想要你」，端出一堆更差的舞台只會讓人誤以為那是市場行情。
*   [x] **薪資（金額層）**：`client/src/engine/salary.ts`。逐季年薪（層級＋當年 d 值）、選秀簽約金、入札金、生涯總收入。**市場規模與 par 分開設定**，六個體系各一份——大聯盟的 par 只比中職高三成，薪資卻是八倍起跳。低於聯盟平均不扣薪（球團簽你的時候不知道你會打成什麼樣），二軍與小聯盟固定薪。
*   [x] **季中交易**：`client/src/engine/trade.ts`。明星的否決權、一般球員的抱怨／沉默、烏鴉的直接打包、重案組之虎的非賣品條款。與合約雙向引用（否決交易讓下一張合約的係數打折），也是逐段成績紀錄的唯一使用者——**一年兩段的逐年表從此真的會出現**，兩段各自帶著自己那支球隊的戰績去切兩本帳。切分用相減而不是各自四捨五入，因此兩段相加精確等於全季。奪冠機率的那一半代價要等職業聯盟的奪冠判定上線。
*   [x] **投手定位細分**：先發／中繼／終結者。**先發與牛棚的分界是體力**（控球那一關拿掉），體力過關之後還要擠得進輪值，而輪值線掛在**球隊戰力**上——在爛隊當先發、去強隊只能進牛棚。牛棚裡由牛棚分（球速為主）對照當年的聯盟線決定關門還是中繼。新增中繼成功（HLD）、中繼王與生涯中繼里程碑；救援王改為終結者專屬。後援第一次有勝敗——舊版讓後援永遠 0 勝 0 敗。見 [ADR 0005](docs/adr/0005-pitcher-rating-by-role.md)。
*   [ ] TJ 量表、手術與打針抉擇
*   [x] **傷病與後遺症**：`client/src/engine/injury.ts`。每季開打前擲一次，結果落在**出賽係數**上——傷病不是一個獨立的懲罰數字，它是「這一季能上場多久」，而出賽量本身就是勝利份額的分母。小傷砍兩到四成半的出賽、有機會留下單項後遺症；大傷是賽季報銷、全能力 −5，兩成機率連隔年也報廢。生涯第二次大傷解鎖帕瓦諾，但 **32 歲以後的大傷是歲月的損耗，不再貼標籤**。魔鬼筋肉人是機率上限、帕瓦諾是下限，兩者並存時取固定值；事件卡自找的風險不受魔鬼筋肉人保護。走 `health` 子序列——那條流從 ADR 0002 保留至今，傷病是它第一個使用者。合約年限的 `injuries` 輸入也第一次有了數字。
*   [x] **跨聯盟轉會**：`client/src/engine/transfer.ts`。四個入口全部接上——挖角、下放遞約、釋出尋路、FA 市場。邊由資料推導，見 [ADR 0004](docs/adr/0004-cross-league-movement-graph.md)。六個體系、94 支球隊、六座名人堂第一次全部可達，難度係數也第一次真的被跨聯盟使用。返台不必特別寫——`fallbackOffers` 自然會列出中職。
*   [x] **入札與海外 FA**：`transfer.ts` 的 `postingBids`／`overseasFaOffers`。入札是合約期間內玩家主動申請、母隊依年資與入札金決定放不放；海外 FA 是合約到期加該體系年資滿七年，不需母隊同意，報價直接併進 FA 市場那張單子。兩者互斥，分界正是合約。資格門檻不另設數字——它就是「落地層級等於頂級聯盟」，那筆錢買的是即戰力。年齡窗口管的是有沒有人出價，因此母隊點頭卻流標是會發生的事。

### 生涯事件

*   [x] **感情系統**：`client/src/engine/love.ts`。國中就可以交往，但求婚要職業＋20 歲。學生時期不累計「婚期一延再延」的風險，改由**關卡**（升學、進職業）處理——還不能求婚的人不該因為沒結婚而被拆散；撐過去的對象會延續到職業，走到結婚就是**青梅竹馬**。校園告白的成功率看當年的大賽名次。五項 legacy 沒有的機制：**旅外的三條路**（帶她走是駝峰、遠距離是平穩的高線、分手乾脆痛一次）、**大傷那一年的陪伴**（隔年報廢 20% → 8%）、**離婚的財務代價**、**感情風波**（吞下去是慢性、分手是重擊）、以及感情與傷病的雙向回饋。獎勵走**當季暫時能力**（[ADR 0006](docs/adr/0006-in-season-temporary-ability.md)）。花樣年華、啦啦隊殺手、青梅竹馬三個特性隨之上線。
*   [x] **養成期的國際賽徵召**：入選判定與奪冠回報（隔季多擲訓練骰）都已接上。
*   [x] **職業期的國際賽徵召**：`client/src/engine/national.ts`。經典賽與 12 強錯開兩年，每兩年一次；**大聯盟球員不打 12 強**。**體育署公文**：第一次徵召起列管五年，期間強制、沒有選項——期滿之後「婉拒」才變成一個真的選擇，而那時你已經三十幾歲、身上有傷。**門檻以中職一軍當年的 par 為基準（＋8），旅外的人用同一把尺**——國籍不會因為旅外而改變。個人成績復用球季模型（傳入國際賽的 par 與場次），不建假層級。榮譽與 MVP 進**總評價分**，與生涯里程碑同一個桶，不進任何單一聯盟。下季受傷風險上升，接上傷病系統；奪冠隔年多擲訓練骰（`championship_bonus.PRO` 終於有使用者）。東亞功夫與 Team Taiwan 兩個特性隨之上線。
*   [ ] 旅外的敘事包裝（球探視角的關注度變化、各國的簽約場景）
*   [ ] 27 種隱藏特性的觸發判定（生涯次數統計 `PlayerState.counts` 已備妥，特性的觸發條件多半看次數）。其中 `legend` / `smallschool` / `grinder` 三個只在結算時判定，隨結算一起做，其餘 24 個仍在這一項。
*   [x] **年度獎項**：`client/src/engine/awards.ts`。明星賽、新人王、年度最佳投手、五項單項王、金手套、守備王、年度 MVP。獎項存成**結構化紀錄**（`year / org / level / code / side`），榮譽字串清單只負責顯示、不參與計分——legacy 用 `h.includes('王')` 比對，任何名字裡有「王」的東西都會被誤判。守備獎項判定看**守備勝率**而非守備分的顯示數字，顯示尺度調整不會影響得獎率。
    *   **中繼王已補齊**（ADR 0005）：中繼成功與中繼定位都上線了，救援王同時改為終結者專屬。

### 結算

*   [x] **勝利份額／敗戰份額雙帳重寫**：`metrics.ts` 的單帳估算改為「責任額 × 勝率」，打擊、投球、守備三分量統一。責任額由球隊總份額推導（每場 3 份，依 52／32／16 切給三段），勝率用畢氏公式。替代水準與 `k` 由各層級當年的 `min`／`par` 推導，不是自由參數。見 [ADR 0003](docs/adr/0003-win-loss-shares-career-score.md)。
*   [x] **生涯評價分**：`client/src/engine/career.ts`。`WS − k × LS` 加上榮譽與里程碑，只算頂級聯盟，每個頂級聯盟各一份。門檻統一為一套，難度由 par 導出的係數在分數端承擔。里程碑分聯盟與生涯兩類。
*   [x] **名人堂票選**：`client/src/engine/hall.ts`。可多聯盟並存；明星帶的人年年入圍卻跨不過門檻，每日以下連候選都進不了。
*   [x] **引退流程與引退場景**：接了兩個玩家選擇點（不願下放而引退、高齡主動引退）。沒進職業的生涯併進同一個出口。第二人生文案原本沒抽出來，已補進 `flavor.json`。續約與 FA 的引退選擇點要等合約系統。
*   [x] **生涯總結畫面**：卡片呈現通算成績、評價分、引退場景、名人堂、里程碑與球迷看板，並在右欄畫出**生涯年表**（投打分兩張表，一段效力一列）。生涯時間軸與分享圖不做，歸「外觀與周邊」。
*   [x] **結算畫面的整理**：引退之後「最近一季」收掉、換成生涯表（年表 ＋ 各聯盟通算 ＋ 一軍通算，高度上限半欄並可捲動）；獎項櫃改名成就櫃，冠上聯盟名並在結算時列出年份。
*   [x] **結算畫面的剩餘部分**：年表含養成六年、欄位與「最近一季」共用同一份定義（各寫一份遲早會分岔）、里程碑進成就櫃。
*   [x] **份額的參照基準錨在球隊戰績上**：真實的 Win Shares 裡球隊的勝利份額總額是「勝場 × 3」，**0 勝的球隊沒有任何勝利份額可分**。個人勝率因此以球隊勝率為基準平移：表現剛好等於聯盟平均的人，勝率就是他球隊的勝率。耦合強度是 `season.json` 的 `team_coupling`（1.0 完全照 James，0 完全不看球隊）。
*   [x] **校準工具**：`client/scripts/calibrate.ts`（`npm run calibrate`，不進例行套件）與 `scripts/guardrail.test.ts`（進例行套件，跑 120 局斷言寬鬆區間）。報表涵蓋分級分佈、建議門檻、獎項取得率、份額三分量佔比與潛力分布。
*   [x] **獎項判定改為「當年門檻線 ± 波動」**：單項王、年度最佳投手與 MVP 都改成「算出當年的線，達到就拿」，取代原本的「達標之後再擲機率」。那條線代表「今年聯盟第一名打到哪」，本來就該年年不同；低於下緣一定拿不到、高於上緣一定拿得到，鬼神門檻因此不必另設。率型門檻寫成 **d 值**，會自動跟著成績模型走。
*   [x] **MVP 改看勝利份額**：原本看 d 值，但 d 值是「他多強」不是「他今年打得多好」。改看份額之後，出賽時間、守位價值、投打合計與球隊戰績會自動一起算進去——那正是 MVP 這個獎的意思。
*   [x] **分級分佈校準**：門檻 **229／182／152／75**，2000 局 balanced（不旅外）樣本實測 **2.1／8.3／24.6／40.0／25.1**，對照目標 2／8／25／40／25。關鍵是校準腳本要算出**考慮保底之後**的門檻，而不是純分位數——保底規則會把拿過獎的人從下面推上來，因此純分位數系統性偏低（每日那一道差了 32 分）。
*   [x] **投球局數改用棒球寫法**：出局數是原子單位，29.1 表示 29 局又一人出局。原本存十進位小數會生出 29.5 這種不存在的局數，而且防禦率與 WHIP 的分母算錯。
*   [x] **養成期的先發／後援與勝敗救援**：依體力判定定位（國中 26／高中 38／大學 44／業餘 48），勝敗由單淘汰的名次直接推導。
*   [ ] **左打的天賦上限修正收窄到打擊**：`abilities.json` 的 `ceiling_modifier` 目前讓左打扣掉整個野手組的天花板，連守備四項一起扣。照設計本意它只該扣打擊——左打的優勢在打席上，跟範圍或臂力無關。要修得讓這條規則改看 `display_groups` 的打擊組。說明已經先改成與實作相符，機制待改。
*   [x] **難度係數的校準**：校準腳本新增「難度係數」段（同一個受試者在每個頂級聯盟各打 400 季）與「跨聯盟」段（旅外樣本的每季評價分與份額勝率）。結論是維持指數 2.0，理由與量到的兩組數字寫在 [ADR 0003 的 Addendum](docs/adr/0003-win-loss-shares-career-score.md)——**係數壓的是「同一個人到處打」的虛胖，不是「各聯盟前段班」之間的差距**，那兩件事的最佳指數分別是 1.75 與 0。
*   [x] **引退的判定順序**：強迫引退只剩年齡上限。被釋出一律先跑尋路（墨聯與澳職因此第一次派得上用場），年齡機率改成引退提問。校準代理要在「身體開口」的那一年掛靴，否則每個人都打到 44 歲、門檻整組漂掉——那條機率原本是強制的。
*   [x] **選秀前的旅外路**：`transfer.ts` 的 `amateurOverseasOffers`。高中畢業的路口有洽談旅日（綜合 ≥ 44，從日職二軍出發）與旅美（≥ 50，≥54 從 1A 否則新人聯盟）。**門檻看綜合的絕對值、落地層級寫死在資料裡**——十八歲的業餘球員沒有所屬聯盟可以相對，而育成合約問的是「他值不值得養」而不是「他現在打得動哪一層」，因此不走 `landingLevel`。
*   [ ] **平衡校準（剩餘）**：
    *   `mrteam`（同隊十五年）的觸發率偏高：40 局探針 28 局拿到，七成。規則照 legacy 實作無誤，問題在自動代理從不轉隊，十五年同隊變成常態。要壓的話動轉隊傾向，不是動門檻。
    *   ~~100% 的生涯都進得了一軍~~ ——與 legacy 的意圖一致，不視為問題。
    *   守位門檻（`defense_average.margin`）還沒校準。
    *   旅外生涯的分級明顯較差（過客四成 vs 不旅外的兩成五）。這是「評價分以聯盟為單位結算」的直接後果，要不要補一條跨聯盟的通算評價是設計問題，不是校準問題。
    *   ~~投手與野手的綜合能力不對稱~~ ——已修（ADR 0005）。同樣的加點總額下投手從 70.0 降到 58.3，野手游擊 55.0；實際生涯的引退評價投手 p50 48、野手約 50。
    *   ~~落地門檻對不上現在的能力尺度~~ ——**刻意保留，不要「修」它**。實測生涯巔峰綜合：野手 p50 50／max 55，投手 p50 55／max 58；而大聯盟的外籍落地門檻是 60、3A 是 56。也就是說大聯盟目前字面上不可及。這是留給**成就點數與天賦**的頭寸——那套系統會提高訓練骰數與事件成功率，能力上限跟著上移，屆時這道門檻才會被推開。現在把門檻壓低等於預支那個成長曲線。
    *   **投手路線的分級分佈高於野手路線**：pitcher 策略實測 10.3／24.3／28.7／33.0／3.8，balanced 是 1.9／7.9／23.9／40.9／25.5。要注意 balanced 的「貼靶」有一半是循環論證——門檻本來就是拿它校的。引擎沒有偏心：**同樣 d 值下野手的份額反而比投手高**（d=+15 時 17.2 對 13.9）；差別在配點經濟——投手 7 項裡 5 項就買到評價公式的 90%，野手 8 項（打擊 4 ＋ 該守位的守備 3 ＋ 體力）要 7 項才到同樣覆蓋率。**不打算把綜合能力塞進評價分**（WS 已經反映了），要收的話只能從配點面下手：攤平變化球的遞減曲線，或讓非該守位的守備能力不計。
    *   ~~純投手 88% 過客~~ ——**那個數字是錯的**。校準腳本的起始守位輪流換，投手策略的樣本裡五分之四是野手在猛練投球能力。已修（`POLICY_POSITION` 把投手策略綁死在 P）。
    *   後援的角色折扣取 19，是「評價相等 → 份額相等」在中段的擬合值。一個常數壓不平兩端（邊緣後援要 17、頂級關門人要 25），因為後援的局數上限讓斜率只有先發的一半。

## 階段一.七：規則編輯器

> 讓所有平衡參數能在介面上直接改，不必動 JSON 檔、也不必重 build。

*   [ ] **參數編輯介面**：讀取 `client/src/data/` 的規則資料，以表單呈現並允許修改。
*   [ ] **機器可讀的欄位描述**：編輯器需要知道每個欄位的型別、合理範圍與意義才畫得出表單。目前 JSON 裡的 `_note` 是給人看的散文，機器讀不懂。需要另立 schema，或改用一套「值＋描述」都能表達的慣例。
*   [ ] **接上自訂規則局的標記**：任何經由編輯器改動過參數的生涯，一律標記為自訂規則局（見 `CONTEXT.md` 與 ADR 0002）——不上天梯、不結算成就點數。

### 這個階段對其他程式碼的約束（現在就生效）

**所有可調參數必須放在 JSON，程式碼裡不得出現寫死的平衡數字。** 編輯器只改得到 JSON；任何留在 TypeScript 裡的常數，使用者永遠調不到。這條規則現在就要遵守，不是等做編輯器時再回頭清——回頭清等於重讀全部引擎程式碼。

判準：如果一個數字改了會影響遊戲平衡，它就該在 JSON。純技術常數（陣列索引、字串前綴、DOM id）不在此列。


## 階段二：進階數據引擎擴充 (Advanced Analytics)

*   [ ] **打擊數據細化**：
    *   計算公式修正：由原本僅計算 H，改為計算 1B, 2B, 3B, HR (`1B = H - 2B - 3B - HR`) 以精確求得 SLG (長打率)。
    *   新增追蹤：BB% (保送率)、K% (被三振率)。
    *   進階數據：OPS (整體攻擊指數)、wOBA 或 OPS+ (需能查詢聯盟當年度平均值進行標準化)。
*   [ ] **投手數據細化**：
    *   新增追蹤：HR Allowed (被全壘打)。
    *   衍生數據：K/9、BB/9、K/BB。
    *   進階數據：ERA+ (標準化防禦率)、FIP (投手獨立防禦率)。
*   [ ] **綜合評價 (WAR)**：建立一套計算勝利貢獻值 (Wins Above Replacement) 的標準公式，供投手與野手進行跨界歷史比較。

## 階段三：跨局繼承與 Meta-Progression

*   [x] **成就檢測器**：`client/src/engine/achievements.ts`。引退時結算，**由既有系統推導**——每一個取得過的特性、每一座獎項、每一次前三名、每一階累積數據都自動成為成就，沒有第二份手寫清單。**同一項成就只給一次 AP**（識別字串因此不含年份），已領過的仍列在清單上但不計分。實測 AP 分佈：差的建構 p50 7、balanced p50 23、pitcher p50 39。
*   [~] **線上化（ADR 0007）**：帳號、成就櫃、天賦、開局登記與伺服器重跑驗證都做完了，前後端接通，`./build-image.sh` 建得出 image。**還差實機部署驗證**——docker-compose 與 Cloudflare Tunnel 的設定寫好了，但從未跑過真的 Postgres，schema 與遷移都還是紙上的。伺服器的測試跑在記憶體假資料庫上（`server/src/fakedb.ts`），驗的是路由邏輯而不是 SQL。原本的規劃：docker-compose 起 Node（同一 image 服務前端與 `/api`）＋ PostgreSQL ＋ cloudflared。同源因此 token 走 HttpOnly cookie；不用 Redis。帳號註冊即通過、不可重複、密碼用 scrypt（`node:crypto` 內建，不必編譯原生模組；相依只剩 `pg` 一個）。
    *   **伺服器重跑驗證**：客戶端上傳重播日誌，伺服器用同一份 TS 引擎重算成就與 AP。前端與 API 同一份建置，因此版本永遠對齊。
    *   **開局登記**：開局時凍結當下的天賦組合並發回識別碼，結算時用那一組重跑——天賦可退款，「現在擁有什麼」與「那一局帶著什麼」是兩件事。
    *   **覆蓋層是全域可變狀態**：伺服器端的驗證必須序列化或隔離到獨立行程。
*   [ ] **成就的持久化**：跨局的已解鎖清單要等存檔層（SQLite / IndexedDB）做好才接得上。目前每一局都視為全新，因此 AP 每局重算。`evaluateAchievements` 的 `unlocked` 參數已經備妥，接上存檔只要把集合傳進去。
    *   **解鎖時間要記真實時間戳，不是局內年份。** 每一局都從 2026 開始，局內年份在跨局的解鎖樹裡沒有鑑別力——每個人都有 2031 年。
    *   **時間戳由存檔層蓋章，不進 `evaluateAchievements`**：那是純函式，同一段生涯必須永遠得到同一份結果。塞 `Date.now()` 進去它就不再可重現，伺服器端也沒辦法重跑驗證。因此 `unlocked` 日後應該從 `Set<string>` 變成 `Map<string, 時間戳>`，而評估器仍然只讀鍵。
*   [x] **設定覆蓋層與天賦資料**：`client/src/engine/overlay.ts` ＋ `talents.json`。天賦宣告「改哪個設定、怎麼改、夾在哪」，**新增一個天賦只是新增一筆 JSON**。首批 15 個分四組（成長／衰退／事件卡／傷病／職業）。天賦寫進 `GameSetup` 與重播日誌——它改變模擬結果，伺服器重跑少了它會得到另一段人生。護欄測試把每一條路徑都解一次（第一次跑就抓到兩個錯路徑）。**與參數編輯介面共用同一層**。見 [ADR 0007](docs/adr/0007-online-accounts-and-server-verification.md)。
*   [ ] **天賦 UI**：
    *   在遊戲開局畫面新增商店介面，允許消耗跨局累積的 AP 解鎖或裝備強力天賦（例如：「斷水流」、「今晚打老虎」）。
*   [ ] **神獸殿堂 (歷史比較介面)**：
    *   讀取歷史紀錄 (Careers Table)，以表格呈現，支援以 WAR、總薪資、冠軍數排序，甚至提供野手與投手混排的總榜單。

## 階段四：深度事件與劇情擴充

*   [ ] **外籍身分的其他規則**：日職「在籍八年視同本土」已經做了（`domestic_after_years`），但它目前只影響**落地層級**。體系內的升降級（`evaluateMovement`）根本不看外籍加成，因此二軍升一軍的難度對本土與外籍是一樣的——真要模擬外籍名額的擠壓，那一層才是關鍵。韓職、墨職是否也該有對應的年限規則也還沒查。
*   [ ] **大學與業餘成棒**：`amateur.json` 的 `cups` 已備妥。在它做完之前，選秀落選等於生涯結束（會走完整的結算流程，不再卡在「尚未實作」）。做完之後落選才有下一條路。
*   [ ] **投手時代／打者時代**：聯盟水準浮動的分項偏移——「今年全聯盟打擊率集體下降」這種細緻模型。基礎的整體浮動已在階段一.五做掉。
*   [x] **加入稀有事件定義**：已在 `events.json` 中加入極度稀有事件的架構與權重 (Weight)。
*   [ ] **實作 Rules Engine**：解析 `traits.json` 與 `events.json` 裡的 `conditions` (觸發條件) 與 `effects` (效果，如 `tj_countdown`, `respect`)。
    *   注意依賴倒置：`traits.json` 目前只有 7 筆 `rules_complete: true`，其餘 20 筆的觸發條件在階段一.五會先寫死在引擎裡。做這一項時那 20 筆要再拆一次——這是刻意接受的重工，因為現階段先做 Rules Engine 會擋住功能對等。
*   [ ] **新事件實裝**：
    *   *禁藥風波*：帶來極端能力增長，但潛藏長年禁賽與名譽掃地的風險。
    *   *組頭接觸*：測試玩家貪念，一發致富或永久被逐出棒球界。
    *   ~~*魔法學校 (Driveline)*：高花費、高能力上限提升，但伴隨極高機率患上「巧克力 (Yips)」。~~ 已實裝——好結果提升力量與球速的上限，壞結果觸發巧克力。
    *   *隱瞞傷勢*：犧牲未來換取當前出賽空間的艱難抉擇。
*   [ ] **情感路線狀態機 (State Machine)**：加入好感度系統，捨棄純隨機觸發，改為累積觸發。

## 階段五：聯盟拓撲 (League Topology)

*   [x] **擴充聯盟資料**：已於 `leagues.json` / `teams.json` / `hall_of_fame.json` 加入韓國職棒 (KBO)、墨西哥棒球聯盟 (LMB) 與澳洲棒球聯盟 (ABL) 的層級、球隊與名人堂。
*   [x] **補齊擴充聯盟的評價門檻**：由 [ADR 0003](docs/adr/0003-win-loss-shares-career-score.md) 的統一門檻表解消。難度改由 par 導出的係數在分數端承擔，因此任何聯盟只要 `leagues.json` 給了 par 與 min 就算得出分級，不必再各生一組門檻。`first_ballot.multiplier` 仍逐聯盟設定——那是各國票選文化的差異，不是難度。
*   [x] **動態升降轉會系統**：改由 [ADR 0004](docs/adr/0004-cross-league-movement-graph.md) 的推導式轉會圖處理。邊不逐條列出，因此逆襲路徑（「在次級職棒打出鬼神成績後被美職重新挖角」）自然成立，不必特別寫。

## 使用者回報批次

原始清單在 `docs/problems.txt`（未編輯的逐條回報，是**輸入**不是待辦）。這一節是把它
分好批的版本，`#` 後的編號沿用原檔條號，方便對照。

分批的原則跟整份藍圖一致：**先規則、再流程、最後外觀**——規則改動會讓畫面上要顯示
的東西整組換掉，反過來做會白做兩次。整批都排在「最後批次：校準與分級門檻」之前，
因為它們仍是規則本身，而校準是對規則的測量。

### 批次 U1：規則正確性（會改變模擬結果）

*   [x] **#1 下放退路要含其他職業聯盟**：接受下放時一併出現他國職棒的邀請，中職只要
    能力到就必定出現（`keepHomeOrg`：排序仍依 par，但不讓 par 把家的門關上）。
    原本還寫著「墨職／澳職門檻要提高，`+4` 偏重、`+2` 可能就夠」——那條已經失效：
    [ADR 0012](docs/adr/0012-import-premium-only-when-clubs-recruit.md) 之後 `+4` 只在球團主動挖角時成立，球員自己找工作不吃加成，
    偏重的來源已經拿掉了。真要再調再開新的待辦。
*   [x] ~~**#16 日本本土球員身分**~~：`domestic_after_years: 8`，待滿之後落地不再吃 `+4`，
    且一軍席次也不再被外籍名額擠壓（[ADR 0019](docs/adr/0019-import-premium-applies-to-top-level-roster.md)）。
    年資只算一軍——現實裡「視同本土」掛在 FA 權上，而 FA 資格算的是一軍登録 145 日
    以上的球季，二軍不進這個計數器。身分留得住，離開再回去仍然算數。
*   [x] ~~**#18 合約內可以拒絕下放**~~：美職五年年資條款——服務滿五年可以拒絕下放，但球
    團可以直接釋出你，機率就是那次下放判定的壓力。其他體系沒有這條規則
    （[ADR 0020](docs/adr/0020-service-time-lets-veterans-refuse-demotion.md)）。
*   [x] ~~**#13 二刀流進職業後拿掉守備能力**~~：**不拿掉**。守備能力還在做事——決定守不守得動守位、賺守備分與守備勝利份額；現實裡二刀流也會守野。改的是評價的尺：原本硬寫 `TWO_WAY_REFERENCE_LEVEL = 'CPBL1'`，旅外二刀流被用中職門檻判定守得動游擊、守位加分給高了，改用 ADR 0021 的 `#benchmarkLevel`。見 [ADR 0023](docs/adr/0023-two-way-keeps-its-glove.md)，commit `f8762d8`。
*   [ ] **「二刀流捕手」在結構上不存在**：二刀流的起始守位必定是 UTIL，而移防掃描
    `[...IF, ...OF]` 兩條光譜都掃不到捕手；起始守位選 C 的人又被
    [ADR 0009](docs/adr/0009-start-position-locks-ability-side.md) 鎖成野手側。兩道規則
    夾出一個死角。現實裡投手兼捕手接近零例，優先度低；真要開，改的是鎖側而不是掃描順序。
*   [x] ~~**#7 閨中蜜友的觸發條件**~~：判定搬到戀情結束的那一刻——三段都以分手收場、且從未結過婚才給，離婚過的永久失格。`kids === 0` 是多餘的判準，刪。見 [ADR 0024](docs/adr/0024-confidante-is-decided-when-the-relationship-ends.md)，commit `f5bafbb`。
*   [x] ~~**#17 「回到熟悉的聯盟」判定錯誤**~~：判定沒錯，**文案錯了**。熟悉度本來就該以
    體系為單位（`TransferOffer.homecoming` 的註解一直是這樣寫的），而報價名單本來就排除
    了現在待的體系，所以那句話成立——他當時人已經不在美職，1A 的資歷確實讓 3A 算回鄉。
    錯在把「體系」說成「聯盟」，玩家於是拿他沒待過的 3A 來對帳。三處文案改成「回到熟悉的
    體系」，`homecoming` 補上「現在不在那裡」的守衛與註解，CONTEXT.md 新增「體系」詞條
    （_Avoid_: 聯盟）。commit `01a5a28`。
*   [x] ~~**#20 `client/src/data` 底下帶 `new` 的檔案**~~：前提已過期，那些檔案早就併進正式
    JSON（葉節點逐一比對，241／39／38／58／31 全數對上，沒有遺失）。重新界定為「資料進了
    JSON 卻沒有消費者」：`traits.dynamic_names` 沒人讀、`love.childhood_sweetheart.trait`
    指向不存在的 `sweetheart`，`App.tsx` 又把 `name === null` 的特性濾掉——`legend` 跳完卡片
    就從面板消失，`mrteam`／`rainbow` 根本沒實作。名稱與配色一律回 `traits.json`
    （ADR 0026），commit `5e4ebf5`。
*   [x] ~~**#21 盤點 JSON 的使用狀況**~~：逐鍵盤點後只有四個沒有消費者。`ability_group_names`
    已被 `display_groups` 取代，刪掉；`PITCH_FAMILIES` 這個手寫常數改成從 `pitch_families`
    衍生；`dynamic_names` 接上引擎；`flavor.placeholders` 是給人看的圖例，保留。守門測試新增
    「特性資料」與「球系」兩組，下一次資料失去消費者會被擋下。commit `5e4ebf5`。
*   [x] ~~**#22 感情事件缺 `kinds` 文案**（含 #28）~~：文案其實有抽，但塞在 `flow.ask()` 的
    `title`——那是 12px 的灰色小標，而且**提問標題不進事件記錄**，玩家只看得到「沒有問出口」
    卻不知道起因。改成敘事合進結果卡的內文（吞下去、分手兩條路各自成為一則完整的故事）。
    見 [ADR 0025](docs/adr/0025-narrative-belongs-to-cards-not-prompt-titles.md)、commit `13f8688`。
*   [x] ~~**#24 事件卡不該加另一側的能力**~~：兩條修法都沒採用——不拆牌組、也不改資料。
    事件卡動得了的能力直接沿用加點看得到的那一份（`#allocatableAbilities`），正負與
    上限提升一視同仁；`rand` 只從本側挑，`pitch` 對野手連骰都不抽。任一面效果被裁光
    的卡退出他的牌堆（48 張裡只有 4 張對純投手退出，野手一張不少），避免「賭贏沒獎、
    賭輸照扣」。順手修好同源的另一半：UTIL／二刀流過去永遠抽不到投手限定卡。
    見 [ADR 0022](docs/adr/0022-event-cards-only-touch-the-side-you-train.md)、commit `6083b46`。

*   [x] ~~**#29 升級卡片一律寫「升上一軍」**~~：標題改成報實際層級名，並依是否為體系
    頂級分 `gold`／`good`——登上大聯盟是里程碑，農場裡的每一階是進度。同源的另外兩處
    一起修：拒絕下放成功的「留在一軍」（那個權利只有美職體系給，這張卡必錯）、生涯
    結算與生涯表的「一軍／二軍通算」（收的其實是所有頂級聯盟與所有小聯盟層級）。
    順手修好空轉的「在二軍與一軍之間來回」測試。commit `c1ba0b1`。

### 批次 U2：流程與輸入缺口

*   [x] ~~**#2 職業後的點數分配**~~：已修好。150 局職業探針：國際賽點數共 272 次分配、
    生涯結束時 `pool` 全部歸零，沒有卡住的點數。
*   [x] ~~**#3 已選守位就別再問非守位的數值**~~：與 #4 是同一件事，併入 #4 一起做——
    養成守位依守備能力自動匹配（「聽教練的」），打擊＋投球成績照記。
*   [x] ~~**#4 養成／二軍要有野手守位**（含 #3）~~：見 [ADR 0021](docs/adr/0021-defense-thresholds-borrow-the-top-league-ruler.md)。
    門檻改成借同體系頂級聯盟的尺，養成／二軍因此有**暫定守位**（`Game.#fieldPosition`，
    每年由 `assignPosition` 現算、不登錄），出賽勞損與守備分都吃它，介面不再自己算一份。
    養成期的純投手守位掛 DH（姓名列仍只寫 `P`），職業的純投手沒有野手成績。

*   [x] ~~**#26 海外合約來得太晚**：現在在中職一路走到「和原球隊簽約」之後才收到海外球團
    的報價，那時候已經沒得選了。海外邀約應該與原隊續約擺在同一個決策點上，或排在它之前。~~
    ADR 0027 · commit `e0e5629`。
*   [ ] **#27 生涯結束的統計畫面守位顯示 `-`**：起始守位選 P，養成階段那幾列的守位欄位
    仍然是 `-`。U2 #4 已經讓養成期的純投手守位掛 DH，生涯表那份顯示沒有跟上。

### 批次 U3：成就與榮譽（資料 + 命名 + 顯示）

整批的骨幹是 [ADR 0031](docs/adr/0031-ladder-achievements-show-the-top-rung-and-sum-the-points.md)：
**階梯型成就一律「顯示最高、點數累加、資料寫增量」**，#6.1／#6.2／#6.6／#9／#23 都是
它的推論。動手前先讀那份 ADR，不要各改各的。

*   [x] **#6.1 成就改成小方塊**：分類先不動；一條階梯只占一格（生涯安打 750/1500 只
    顯示 1500），滑鼠移過去顯示 AP 點數。排序**固定**：依聯盟階梯、生涯放最後，累積類
    同樣遵守，不再用「最新在前」（`Account.tsx:277` 那條註解與它的理由一併拿掉——那是
    結算畫面的工作）。**未解鎖的一律不顯示**，也不畫灰底占位；空的分類整組不出現，沒
    去過墨聯就沒有墨聯那一區。`Account.tsx:246` 的「成就 N」改成數**收斂後的格數**：
    500/1000/1500 安只算一項。
*   [x] **#23 累積成就改成固定級距**：安打每 500 一階，不要 750/1500/2500 這種不規則
    級距；全壘打、打點同一套邏輯。**級距等寬，但每階的增量遞增**——形狀是
    `min(ceil(n/2), C)`，愈後面愈值錢但有上限。`points_per_rung` 與
    `display_divisor` 隨之走人。第一階、級距與 `C` 的實際數字屬於校準，見最後批次。
    與 #6.1 是同一份資料，一起改。
*   [x] **#6.2 養成成就只顯示最高**：拿冠軍就自動獲得亞軍、季軍的點數。這條規則同時
    套用到 `international`、`tier`、`cumulative`——`tier.by_tier` 的 `[10, 6, 3, 1, 0]`
    是舊的「內含」寫法，要改寫成增量（數字延到最後批次）。
*   [x] **#6.3 中英之間補空格**：「中華隊WBSC」→「中華隊 WBSC」。從 `#addHonor()` 組
    字串的源頭修（`game.ts:941`、`game.ts:4639`），不要在顯示層補。
*   [x] **#6.4 職業國際賽不加年份**：與養成階段一致。年份不進識別字串，名字裡也不該
    有——`achievements.ts:138` 那個 `honor.replace(/^\d{4}\s*/, '')` 是在事後擦，源頭
    改乾淨之後它就該消失。
*   [x] **#6.5 獎項要顯示聯盟**：**一律**加聯盟前綴，用 `leagues.json` 的
    `top_league_names`（中職／日職／韓職／墨聯／澳職／大聯盟）。有專屬名稱的也照加
    （「大聯盟 賽揚獎」「日職 澤村賞」）——不設例外，新增聯盟或 alias 時不會漏。
*   [x] **#6.6 國際賽全部歸到國際賽分類**：中華隊 PONY、中華隊 WBSC U-15、世界 12 強、
    世界棒球經典賽 MVP，命名統一成 `honor_prefix + 賽事名 + 名次`，養成與職業同一套。
*   [x] **#9 釐清「生涯分級 每日」**：查清楚了——「每日」是 `hall_of_fame.json:27`
    `labels` 的第三級，指**每日先發**的主力，`career.ts:304` 拿它當名人堂門檻。不是
    廢成就，是標籤太短讀不出原意。**改 `labels` 本身**成
    `["名人堂", "明星", "每日先發", "替補", "過客"]`，成就名、升降級卡片、名人堂畫面
    一次修好；`hall.test.ts` 與 `career.test.ts` 裡的字串跟著改。

### 批次 U4：介面重排（與階段一的「UI 元件化」同時做）

*   [x] ~~**#12 拿掉右側的當年數據方格**、**#14 年薪顯示在 SEED 上方**、**#15 合約剩餘
    年份移到奪冠機率左邊**（三項同一次做完）~~：見
    [ADR 0030](docs/adr/0030-the-scoreboard-shows-what-the-engine-judges.md)。右欄七格整排
    移除，右欄只留最近一季／生涯表；聯盟水準與學年／職涯**整個不再顯示**——ADR 0029
    之後升降級卡片自報「綜合 X／門檻 Y」，常駐一個不是判定用的 par 只會讓玩家去對一條
    不存在的線。年薪＋生涯收入併成 SEED 上方一行、合約年數移到姓名上方與奪冠機率對稱、
    投手側／野手側併進「綜合」格（單邊寫 `overall`＝判定值，二刀流寫兩數字「投／野」）。
    **明確否決**：二刀流的下放判定改用兩側平均——那會懲罰多能，場次稀釋該由
    `fieldingShare` 與成績承擔，不在門檻上再罰一次。commit `0a11f2d`。
*   [x] **#5 生涯結束的榮譽移到右側狀態下方**：不要放在左側能力下方（`#panel-abilities`
    底下的 `HonorBoard`），行高也太小。**生涯結束時右欄的 `StatsPanel` 整塊拿掉**——
    狀態、生涯表、榮譽三者都放進事件流。理由：那三塊都是「這段生涯的結論」，結論屬於
    敘事的結尾而不是常駐儀表板；引退之後「最近一季」也已經沒有下一季可比，留著一個
    `max-height:50%` 的面板只是把結局壓進半個畫面裡捲。生涯進行中右欄維持最近一季＋
    狀態兩塊不變。
    作法：`LogEntry` 加一種結構化的結局項目（帶著 summary），`LogView` 遇到就渲染現有的
    `CareerTable`／`TraitList`／`HonorBoard`。不要讓引擎把表格寫成 HTML 字串——那張表帶
    欄位提示、要橫向捲動、還要標受傷年份（#19），寫進引擎等於把畫面搬進引擎，`esc()`
    的負擔與出錯機會都跟著進去。結構化項目也讓重放日誌維持「只存事實、顯示物全部重算」。
    代價是 `LogView` 從兩種形狀變三種。結局是**三張獨立的卡**，依序 狀態 → 生涯表 → 榮譽；
    同一個 kind 帶段落別（traits／career／honors），不是三種新 kind。
    「最近一季」直接消失——引退後沒有下一季可比，最後一季的數字已經在生涯表的最後一列。
    `HonorBoard` 本來就 `if (summary === null) return null`，只在生涯結束出現，移走不會在
    生涯進行中留下空缺；`TraitList` 相反，整段生涯都留在右欄，只是結束時「也」進結局卡。
*   [x] **#19 受傷年份標色**：生涯表裡用半透明淡紅底表示。這不是純 CSS——`SeasonRecord`
    目前沒有任何傷病欄位，要先加一個（例如 `injured: 'minor' | 'major' | 'rehab' | null`），
    存檔是重放日誌（ADR 0002）所以加欄位不必相容舊檔。標的是**成績被傷壓過的年份**：
    `rollInjury` 擲出 minor／major 的那年，加上 major 的 rehab 拖到的下一年——共同點是
    `seasonFactor` 真的少了。深淺區分大傷小傷。**明確否決**：只標事件發生當年——那會讓
    rehab 那年看起來像無緣無故的崩盤，而標色要回答的正是「這年的數字為什麼難看」。
    `#injuryRisk` 不算，它擲完就歸零，只是機率不是傷。
*   [x] **右欄的「當年數據」標題整個拿掉**：`#panel-stats` 寫死一個 `<h4>當年數據</h4>`，
    底下 `StatLines` 又用 `label` 印一個 `<h4>最近一季</h4>`，兩個同級標題連在一起，
    中間沒有任何內容。留下的是「最近一季」——面板標題寫「當年」本來就是騙人的（季初
    訓練時底下放的是去年的成績，那塊的註解自己也這麼寫）。拿掉之後右欄是兩個平級
    區塊：最近一季 → 狀態。
*   [x] **#8 特性滑鼠移過去顯示解釋**：功能其實已經在了（`App.tsx` 的特性標籤帶
    `title={t.effect_text}`），但要**改成點擊展開的自繪 tooltip**，標籤本身也要看得出來
    可以點。原生 `title` 在手機上永遠不會出現，而且延遲約一秒、樣式不可控、長文字會被
    截；`effect_text` 是玩家判斷要不要留著負向特性的主要依據，不能只存在於滑鼠環境。
    `title` 留著當桌面的額外好處。**明確否決**：只加個視覺提示、繼續靠原生 title——那
    等於承認觸控玩家看不到負向特性的說明。
*   [x] **大賽點數改成一次付滿該級成本**（做 U4 時當場發現的引擎缺陷）：見
    [ADR 0032](docs/adr/0032-a-die-spends-its-face-a-point-buys-a-level.md)。原本兩個
    點數來源共用「一次投 1 點」，導致能力 50 以上按下大賽點數只會看到蓄力槽 +1、
    能力一動也不動；64 以上要連按六次。改成大賽點數照級價付（選項上寫出 `－6 點・`
    的價碼、`#poolPriceOf()` 先折抵蓄力槽），**訓練骰維持照骰面投**——骰面是遊戲給
    的，級價是遊戲收的，兩者不該互相截斷。連帶：`#pool -= value`／復原時 `+= value`、
    分配標題改報餘額、`ENGINE_VERSION` 滘到 2（伺服器用 `Game.replay` 重驗成績，選項
    意義變了不滘版本等於讓舊分頁換一組 AP）。**立成規矩：改動既有選項的意義就滘版本**。
*   [x] **榮譽榜分組小標與行距**：原本三類標籤（獎項／里程碑／業餘國際賽／人生）全部
    擠在一段裡，只靠邊框樣式區分——而邊框樣式在 `.tag` 上本來就被 tone 佔用了，玩家
    分不出哪一批是什麼。改成四個 `.tag-group`，各帶一行 `.fin-caption` 小標，空組不
    渲染。行距用 `.tag-row{gap:8px}` 搭配 `.tag-row .tag{margin:0}`——`.tag` 自帶
    `margin:0 2px`，直接下 `gap:4` 會變成橫向 8px、縱向 4px，看起來就不是同一個格線。
*   [x] **#10 天賦頁改成橫條 UI**：頭尾 0-3，左鍵 +1 級、右鍵降級，等級下方顯示升級
    所需 AP；一次點滿該級所需的 AP，不能分次點。**右鍵是降一級，不是整條退光**——橫條
    的左右鍵必須互為反向操作，否則玩家會在自己的存檔上按出一個無法用同一顆鍵還原的
    動作。因此 `refundTalent` 要從「退到 0 級」改成「退到第 N 級」，伺服器端與那條把
    每個天賦每條路徑都解一次的護欄測試都要跟著改。退款仍是全額、沒有價差，所以在
    邊界上來回點也不會刷出 AP。
*   [x] **#11 「天賦商店」改名為「天賦」**：玩家看得到的只有三處（`Account.tsx` 的分頁
    標籤、未登入時的 `title`、`game.ts` 引退結算卡片上的「下一段生涯開場的天賦商店
    消耗」），但**文件、註解與資料檔 `_note` 一律跟著改**。這個專案只有一份
    `CONTEXT.md` 詞彙表，它存在的意義就是畫面上的詞與程式裡的詞是同一個詞；UI 叫
    「天賦」而文件叫「天賦商店」，下一個讀文件的人會以為那是兩個畫面。`talents.json`
    的 `_note` 現在就同時用著兩種講法。**明確否決**：註解保留「商店」以表達「那是花
    AP 的地方」——那個資訊該由詞彙表的定義承擔，不是靠名字夾帶。
    實作時連程式碼裡的識別字一起改（`TalentShop` → `TalentPanel`、分頁 key `'shop'` →
    `'talents'`），理由同上：#10 要重寫這張畫面，讓它從一開始就用最終詞彙。含「商店」
    的句子逐句重寫而不是整批取代——「在天賦商店消耗」直接抽掉兩個字會變成「在天賦
    消耗」，所以改寫成「兌換天賦」「天賦那一頁」。**唯一的例外是 `docs/adr/` 底下的
    既有 ADR**：那是寫下當時決定的紀錄，不是現況說明，改掉等於竄改決策時的用詞；
    要推翻它們的是新的 ADR，不是回頭編輯。CHANGELOG 反過來要改——那幾條還在
    `[Unreleased]`，出貨時得跟畫面上的詞一致。

### 批次 U5：grill 期間新發現（不在 `problems.txt` 裡，因此沒有條號）

這些是做 U3／U4 的 grill 時當場浮出來的。放在 U4 之後而不是插隊，理由跟整份藍圖一致：
它們沒有一項會擋住 U4，而 U4 正在重排的版面就是其中兩項要顯示的地方。

*   [ ] **生涯（職業）逐年顯示國際賽成績**：目前國際賽的結果只變成一條榮譽字串，而
    ADR 0031 之後那條字串**不帶年份**（帶了的話同一項成就每年都會被當成新解鎖）。
    所以「哪一年代表隊拿了什麼」現在沒有任何地方看得到——它該是生涯年表的一欄或
    一組列，跟成就是兩件事：成就問「這輩子做到了嗎」，年表問「哪一年做到的」。
*   [ ] **養成期的守位一律顯示成 DH，蓋掉玩家選的起始守位**：選一壘手開局，入學卡
    寫的是一壘手，記分板卻寫 DH。成因不是記分板，是 `game.ts` 的 `#fieldPosition`
    先問 `#playsField`，而養成期那個判斷是 `rating.fielder > rating.pitcher`——
    **投手側評價只要打平就被歸成純投手**，純投手在養成期一律掛 DH（那是為了讓打擊
    成績有位置可標）。要決定的是：養成期到底該不該用「哪一側評價高」來推守位，還是
    該照玩家選的起始守位走到選秀為止。與「二軍出賽數會因暫定守位而漂移」同源，兩條
    一起看。
*   [ ] **「起始守位只決定你的天賦往哪邊長」那張卡只該對 UTIL 出現**：`game.ts:902`
    無條件跟在入學卡後面。玩家已經指名一壘手時再告訴他「不決定你只能練那一邊」，
    講的是他沒問的問題；那段話是寫給選「守位不定」的人看的。

### 批次 U6：第 33–53 條回報

這一批進來得晚（`problems.txt` 的 #33 起），大半在天賦與成就的 grill 裡當場修掉了，
逐條的結案標記寫在 `problems.txt`，這裡只留還沒關的三條。原檔的 #45／#46 各出現兩次
（條號重複），對照時以內容為準。

*   [ ] **#39 隱藏事件「三人行」**：劈腿時有機率觸發開放式關係，等同再結一次婚、多
    一次 buff 機會——**劈腿唯一的好結局**。`love.json` 目前沒有這條路，劈腿只有壞
    結局。要決定的是它算不算一段婚姻（`divorces`／閨中蜜友的失格判定 ADR 0024 都會
    被牽動），以及開放式關係破裂時走哪一條分手曲線。
*   [ ] **#43 累積數據冠「生涯」前綴**：待釐清指的是哪一份——年表下的「各聯盟通算／
    頂級聯盟通算」、結算卡的「所有頂級聯盟通算」，還是成就櫃的累積階梯。三處現在都
    叫「通算」，一起改或分開改是兩種答案。
*   [ ] **#48 結婚對象的獨立屬性**（部分完成）：`love.json` 的 `partners` 已有各自的
    側寫與能力加成，`88cf063` 讓娶不同對象各算一項成就。**還沒分的是機率與費用**：
    風波發生機率、生小孩機率、搬家與離婚的花費仍是全域值。側寫裡已經寫出了「她怎麼
    花錢、想不想要孩子、定不定得下來」，那三句話目前不對應任何數字——這是資料先於
    規則的缺口，不是新功能。

## 最後批次：校準與分級門檻

grill 期間浮出、刻意延後的項目。都屬於「測量與靶心」而不是規則本身，因此排在所有
規則批次做完之後——規則還在動的時候調門檻等於追移動靶。

**這一批不要提前動手。** 做批次 U1-U4 時如果撞到取得率、門檻、護欄數字不對，把
發現寫進這一節當待辦就好，不要當場改。

*   [ ] **#25 今晚打老虎的取得機率偏高**：使用者體感太常拿到。屬於取得率而非規則，
    等 U1-U4 做完再一起看事件卡的權重分佈。

*   [ ] **校準樣本涵蓋各起始守位**：現行 `npm run calibrate` 的 balanced 樣本沒有 UTIL
    起點，因此 `ratingPosition` 的工具人推定（見 [ADR 0008](docs/adr/0008-two-way-fielder-rating-as-batting-plus-position-bonus.md)）
    在校準數字上完全看不出來，只有單元測試證明它有效。守位相關的改動目前是**盲改**：
    看不到分佈反應。要決定的是讓校準跑遍各起始守位、還是另立一支守位專用的探針。
*   [ ] **二軍出賽數會因暫定守位而漂移**：U2 #4 把二軍／養成的推導守位從「起始守位直接
    對映」換成「借一軍門檻的 `assignPosition`」，而那個位置一直都在餵 `playSeason`
    的出賽勞損（`game.ts` 的 `pro.position ?? ratingPosition(...)`）。守備好的 1B 起點
    會被當游擊算勞損，二軍出賽數往下走。改完之後量一次二軍的場數分佈再決定要不要動。
*   [x] ~~**收回名人堂護欄的暫放值**~~：已收回 12%。當時的破線並不是玩家變強，而是
    取樣策略有兩個毛病（見下），修好之後是 8.3%，12% 不再吃緊。
*   [ ] **名人堂護欄又破了，而且這次是玩家真的變強**：跑 U 批次期間量到 21.8%，
    做完「事件扣點改走成本曲線」（[ADR 0033](docs/adr/0033-event-penalties-follow-the-cost-curve.md)）之後是 30.3%。
    後面那 8.5 個百分點是預期中的：扣點從 1:1 改成吃成本曲線，高能力段的下檔
    變輕，球員生涯末段守得住能力。**這是規則的正確化，不是崩壞**，所以按本節的
    規矩不當場調門檻——但 21.8% 那一段是哪裡來的還沒查，先記著。分級門檻的校準
    建議（274/217/154/64，現行 191/146/104/41）要在這之後重跑一次再定。
*   [ ] **階梯成就的 AP 數字全部重訂**：[ADR 0031](docs/adr/0031-ladder-achievements-show-the-top-rung-and-sum-the-points.md)
    把所有階梯的數字語意改成「增量」，`tier.by_tier` 的 `[10, 6, 3, 1, 0]` 與
    `international.by_rank` 的冠 5／亞 3／季 2 是照舊語意訂的，直接累加會翻倍
    （名人堂級 20 點、國際賽冠軍 10 點）。要一起定的還有 #23 的三個參數：各項的第一階、
    級距、以及增量上限 `C`。**一次校準完**——這幾組數字彼此換算，分開調會互相追。
    靶心是天賦的定價：一段中庸的生涯該買得起什麼，一段名人堂生涯又該領先多少。
*   [ ] **分級門檻與獎項保底**：現行門檻 191/146/104/41，校準建議 **246/220/153/66**。
    名人堂帶實際 8.1%／目標 2%。但**門檻不是第一個該動的旋鈕**——獎項保底規則
    （拿過 MVP 或年度最佳投手直接保到明星帶）會先決定分佈形狀。順序是先檢討保底、
    再調門檻。
*   [ ] **成績本身不現實**：使用者實測「普通玩很難上明星」，與模擬的分佈對不上。
    在調門檻之前要先確認量出來的成績像不像真的球員成績——門檻是把分佈切段，
    切錯段的前提是分佈本身可信。這一項排在門檻之前。

    **進度（2026-08-21）**——這一項拆成三段，出賽量那一段做完了：

    *   [x] **出賽量**：`stamina_factor` 改成錨點表，健康球季的出賽率 p50 從 74% 抬到
        84%（真實先發球員約 90%），門檻改由聯盟 par 推導。見
        [ADR 0013](docs/adr/0013-stamina-sets-games-played-scaled-on-the-ability-axis.md)。
    *   [x] **能力軸上半段的死區**：門檻以上的體力原本什麼都換不到，改成折抵受傷
        機率，零點是各守位自己的打滿門檻。見
        [ADR 0016](docs/adr/0016-saturated-stamina-converts-to-injury-resistance.md)。
        這一段是順手補的，不是分佈不真實的原因。
    *   [ ] **獎項取得率**（進行中，也是真正的根因）：起點是 71% 的生涯拿過年度最佳
        打者、平均每局 2.91 座——一個十五年的生涯裡有五分之一的年份是全聯盟最佳打者。
        **對手模型太弱。** 三份決策都已落地：獎項的線改由對手池的極值推導
        （[ADR 0014](docs/adr/0014-award-lines-derive-from-extreme-value-of-the-rival-pool.md)）、
        取得率的靠點放在球季層而非生涯層（[ADR 0015](docs/adr/0015-award-rates-anchor-at-the-season-not-the-career.md)）、
        打者側的累積型獎項（打點王／盜壘王）也一併入池
        （[ADR 0017](docs/adr/0017-award-lines-from-the-rival-pool.md)）。

        **量到哪了**（`CALIBRATE_RUNS=2000`，單季取得率，聯盟母體加權）：MLB 的
        `mvp` 1.2%／`batting_king` 1.3%／`gold_glove` 1.6%／`hr_king` 1.6%／
        `steal_king` 1.8%／`batter_of_year` 1.9%／`rbi_king` 1.9% 都進了 1-3% 帶，
        次級聯盟依序放寬（NPB1 的 `rbi_king` 5.5%、LMB 7.3%），方向正確。

        **`obp_king` 12.9% → 0.7%**：根因不是 `band` 太窄，是上壘率的分子漏了故意
        四壞，而門檻線那側同樣漏算，偏差不互相抵銷。順帶把恐懼值改成只吃 d，敬遠
        因此在低階聯盟也存在。見 [ADR 0018](docs/adr/0018-intentional-walks-count-toward-on-base.md)。
        現在反而略低於同儕的 1.3-1.9%，但那要等 `band` 整批校準時一起收。

        **還沒收的一個**：`all_star` 55.6%。它是 `base 28 + per_d 7 + clamp 92` 的
        獨立機率式，沒有走對手池。兩個毛病：`base 28` 是中職的名額比例（明星賽 50 席
        ÷ 一軍名冊約 168 人）卻套用在每個聯盟上，大聯盟該是 64 ÷ 780 ≈ 8%；而且線性
        式在 **d≈9 就打到 92% 的天花板**，d=9 與 d=80 沒有任何差別。

    - [ ] **`all_star` 名額比例改成逐聯盟**：`base` 不該是全聯盟共用的常數，該由
          `leagues.json` 的名冊規模推出來（中職 ≈30%、大聯盟 ≈8%）。
    - [ ] **`all_star` 的線性式在 d≈9 撞頂**：`clamp 92` 讓 d=9 和 d=80 沒有差別，
          頂級聯盟的明星席位應該還要能區分強弱。
    - [ ] **`band` 整批收斂**：`obp_king` 現在 0.7%，略低於同儕的 1.3-1.9%。

    **目前紅著的護欄**：`scripts/guardrail.test.ts` 的名人堂比例 21.0%（ADR 0027／0029／
    0028 季末改序之後重測；U1-U4 之後是 19.3%，更早記的是 17.6%），斷言上限 12%、
    目標 2%。數字每次規則批次都在跳，是因為抽 `asksRetirement()` 的位置一動，同一顆
    種子的生涯長度就變——**這正好說明旋鈕在生涯長度，不在名人堂門檻**。獎項入池沒有動到它
    （改前改後都是 17.6%）——**名人堂帶吃的是
    累積 Win Shares，不是獎項數**，所以真正的旋鈕在保底規則與分級門檻，不在獎項線。
    **不要去放寬斷言。** 順序仍然是：獎項取得率（剩 `all_star`）→
    保底規則 → 分級門檻。

### 已修：兩個取樣毛病（記在這裡，因為它們解釋了上面的數字為何變動）

*   `scripts/guardrail.test.ts` 與 `scripts/calibrate.ts` 的 balanced 能力清單全是野手側，
    但起始守位輪替裡有 `'P'`。鎖側（[ADR 0009](docs/adr/0009-start-position-locks-ability-side.md)）之前 P 起點會被
    默默判成野手，**這套取樣從來沒產生過投手**——報表上「投球 目標 35%／實際 0.0%」
    就是這麼來的。改成依起始守位選能力側之後是 16.7%。
*   護欄漏了校準已有的「身體開口就掛靴」規則，代理一路硬撐到年齡上限（生涯中位數
    27 個球季）。生涯評價分是累積 Win Shares，打三十年當然進名人堂：名人堂帶因此
    虛胖成 16.7%，補上之後 8.3%，與校準的 8.1% 對齊。
