# 專案開發藍圖 (Roadmap)

《棒球人生模擬器 2.0》**還沒做的事**。這是專案唯一的待辦清單。

- 做完就從這裡刪掉。為什麼這樣做寫進 ADR，玩家看得到的差別寫進 `CHANGELOG.md`。
- 2026-09-25 以前的完整藍圖（含已完成條目與當時的理由）在 git 歷史裡，最後一版在 commit `ce85924`。
- `#N` 是玩家回報的條號。原始回報清單已經刪除，最後一版在 commit `4c00592`（`docs/problems.txt`）。

## 功能

*   [ ] **UI 元件化**：`client/src/App.tsx` 還有兩千六百多行，開始畫面、事件紀錄、狀態欄、能力表、成績表都還沒拆成獨立元件，主題與周邊視覺也還沒動。
*   [ ] **二刀流的合併生涯紀錄**：投打成績分開累積、分開顯示、分開評獎，另開一頁「合併生涯紀錄」，用 WS／WAR 這種能跨投打加總的指標（WAR 已經有了，見 `career.ts` 的 `warOf`；它是聯盟內的數字，跨聯盟要另外決定乘不乘難度），讓二刀流能和單一領域的球員同榜比較。見 `hall_of_fame.json` 的 `two_way`。
*   [ ] **神獸殿堂（歷史比較）**：天梯已經能依評價分與薪水排序；還缺 WAR 排序、冠軍數，以及野手投手混排的總榜。WAR 已經算得出來，但它是聯盟內的數字，跨聯盟的總榜要決定乘不乘難度係數。
*   [ ] **旅外的敘事包裝**：球探視角的關注度變化、各國的簽約場景。
*   [ ] **業餘成棒**：`amateur.json` 的 `cups.AMA` 已經備妥，流程還沒接上。
*   [ ] **投手時代／打者時代**：聯盟水準浮動的分項偏移（例如「今年全聯盟打擊率集體下降」）。整體浮動已經做了。
*   [ ] **情感路線狀態機**：加入好感度，捨棄純隨機觸發，改成累積觸發。
*   [ ] **Rules Engine**：解析 `traits.json` 與 `events.json` 裡的 `conditions` 與 `effects`。目前大部分特性的觸發條件寫死在引擎裡，做這一項時要再拆一次，這是刻意接受的重工。
*   [ ] **「二刀流捕手」在結構上不存在**：二刀流的起始守位必定是 UTIL，移防掃描的兩條光譜都掃不到捕手；起始守位選 C 的人又被 ADR 0009 鎖成野手側。現實裡投手兼捕手幾乎沒有，優先度低；真要開，改的是鎖側，不是掃描順序。

## 規則編輯器

讓所有平衡參數能在介面上直接改，不必動 JSON、也不必重新 build。前提是參數全部放在 JSON（見 `DEVELOPER.md`）。

*   [ ] **參數編輯介面**：讀取 `client/src/data/` 的規則資料，用表單呈現並允許修改。
*   [ ] **機器可讀的欄位描述**：編輯器要知道每個欄位的型別、合理範圍與意義。JSON 裡的 `_note` 是寫給人看的，需要另立 schema，或改用一套能同時表達「值＋描述」的慣例。
*   [ ] **自訂規則局的標記**：參數被編輯器改過的生涯一律標記為自訂規則局（見 `CONTEXT.md`），不上天梯、不結算 AP。

## 最後批次：校準

這一批都是「量測與靶心」，不是規則本身，排在所有規則改完之後；規則還在變的時候調門檻，等於在追移動靶。**不要提前動手**：做其他批次時如果發現取得率、門檻或護欄數字不對，記在這裡就好，不要當場改。

下面的數字多半是 2026-08 量的，後來規則改了很多，動手前每一條都要先重量。

### 取得率與獎項

*   [ ] **#25 今晚打老虎的取得率偏高**：玩家體感太常拿到。等其他批次做完，和事件卡的權重分佈一起看。
*   [ ] **`all_star` 名額比例改成逐聯盟**：`base` 是全聯盟共用的常數，應該由 `leagues.json` 的名冊規模推出來（中職約 30%、大聯盟約 8%）。原本「線性式在 d≈9 就撞頂」的問題已經在 `3df64e5` 改成次方曲線時解決。
*   [ ] **`band` 整批收斂**：`obp_king` 的單季取得率 0.7%，略低於其他單項王的 1.3–1.9%。
*   [ ] **先檢討獎項保底，再調分級門檻**：拿過 MVP 或年度最佳投手就直接保到明星帶，這條規則會先決定分佈的形狀，門檻不是第一個該動的旋鈕。調之前先重量名人堂帶；舊紀錄已經過期，而且方向反了（上次量到 0.8%，低於 2% 的目標）。

### 校準樣本

*   [ ] **balanced 樣本不練臂力與配球**：`client/scripts/harness.ts` 的 `BALANCED_FIELDER` 只有六項，`arm` 與 `cat` 一次都沒練到。補進去之後捕手從 73 季跳到 283 季，名人堂帶從 0.8% 變成 1.7%。要先決定哪一種才算「平衡型玩家」，補進去會讓所有校準數字整批位移。
*   [ ] **校準樣本涵蓋各起始守位**：balanced 樣本沒有 UTIL 起點，工具人的評價推定（ADR 0008）在校準數字上完全看不到，守位相關的改動現在等於盲改。要選的是：讓校準跑遍各起始守位，還是另外寫一支守位專用的探針。

### 養成期

*   [ ] **選秀門檻過鬆**：`draft.rounds.tiers` 是照三年高中制校準的，六年制把畢業時的綜合能力從約 38 拉到約 46，輪次全面前移；當時實測 200 段生涯全部被指名，簽約金級距形同虛設。大學路線上線之後要重量。2026-09-26 中職一二軍的 par 與 min 各 +2（ADR 0054），指名之後落在二軍卻打不上場的人會變多，重量時一起看。
*   [ ] **養成期打擊率過高、分佈過窄**：當時打擊率中位數 .482（p10 .453／p90 .517）。可以調的旋鈕有三個：各階段的 `par`（對手多強）、`hit_rate.per_point`（能力換成績的效率）、噪音（短期賽的運氣成分）。要先確定失真來自哪一個。
*   [ ] **大賽與國際賽門檻**：名次門檻整體 +8 之後，每段生涯的冠軍數從約 5.5 掉到 0.88、國際賽從 4.19 次掉到 0.95 次，約半數玩家整段生涯沒有代表過台灣；+4 或 +6 可能才是甜蜜點。國中國際賽的觸發率仍然是高中的兩倍多，兩件事都是門檻問題，要一起處理。
*   [ ] **國際賽奪冠的訓練骰只有約 10% 的生涯碰得到**：要調的是國際賽的資格門檻，不是加成的顆數。和上一條一起看。
*   [ ] **二刀流的綜合能力只取較高的一側**：另一側完全不計，因此能投能打的高中生在選秀評價上不佔便宜，但現實中這種球員對球團更有價值。

### 職業期

*   [ ] **生涯打擊率分佈太窄**：當時量到 p10 .294／p90 .325。測試用的配點策略都一樣，要先用多種 build 重測，才知道是模型的問題還是量測的問題。
*   [ ] **同邊／反邊對決還沒接上賽季模擬**：左手的優勢現在只有聯盟尺的比例折扣，四大球系的同邊優勢與反邊剋星（見 `docs/design/formulas.md`〈投手的四大球系〉）還沒進成績公式。
*   [ ] **`mrteam`（同隊十五年）觸發率偏高**：40 局探針有 28 局拿到。規則沒錯，是自動代理從不轉隊；要壓的話該動轉隊傾向，不是門檻。
*   [ ] **落地門檻改看 par 之後重量旅外**：ADR 0054 把球團主動的落地門檻從 min + 4 改成 par（日韓一軍 par + 2）、墨聯澳職改成退路聯盟，旅外人數、落地層級、簽約金都會位移，還沒量過。
*   [ ] **旅外生涯的分級明顯較差**：過客四成，不旅外的是兩成五。這是「評價分以聯盟為單位結算」的直接後果；要不要加一條跨聯盟的通算評價是設計問題，不是校準問題。
*   [ ] **投手路線的分級分佈高於野手路線**：引擎沒有偏心，差在配點經濟（投手 7 項裡 5 項就買到評價公式的 90%，野手要 7 項）。不打算把綜合能力塞進評價分；要收的話從配點面下手：攤平變化球的遞減曲線，或讓非本守位的守備能力不計。
*   [ ] **後援的角色折扣只有一個常數**：目前取 19，是中段的擬合值，兩端壓不平（邊緣後援要 17、頂級關門人要 25），因為後援的局數上限讓斜率只有先發的一半。
