# 複製成 .env 再填。前三個是必填，沒填 compose 會直接拒絕啟動；APP_PORT 可以留空。
POSTGRES_PASSWORD=
# openssl rand -hex 32
SESSION_SECRET=
# Cloudflare Zero Trust → Networks → Tunnels 取得
TUNNEL_TOKEN=
# 本機驗證用的埠（只綁 127.0.0.1）。不填就是 8080。
APP_PORT=
