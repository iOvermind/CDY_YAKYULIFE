# 複製成 .env 再填（`./deploy.sh` 也會在沒有 .env 時自動產生一份，金鑰隨機生成）。
# compose 會自動讀同目錄的 .env，不必手動指定。
#
# 前三個是必填，沒填 compose 會直接拒絕啟動。

# openssl rand -hex 32
# 改了之後舊的資料庫連不上——那顆密碼在初始化時就寫進 ./data/pg 了。
POSTGRES_PASSWORD=
# openssl rand -hex 32
# 沒設的話伺服器會用隨機金鑰，重啟後所有人被登出。
SESSION_SECRET=
# Cloudflare Zero Trust → Networks → Tunnels 取得。
# 通道的 public hostname 那一欄，service 填 http://app:8080。
TUNNEL_TOKEN=

# 本機驗證用的埠與綁定位址。預設只綁 127.0.0.1——對外走 tunnel，而綁 0.0.0.0
# 等於在公網上開一個沒有 TLS 的服務。
APP_PORT=8080
BIND_ADDR=127.0.0.1

# 資料放哪。預設是 repo 底下的 ./data。
DATA_DIR=./data

# 要跑哪個 image。預設就是 ./deploy.sh 建出來的那一個。
APP_IMAGE=cdy_yakyulife:latest
