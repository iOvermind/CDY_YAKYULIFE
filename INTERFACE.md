# 棒球生涯模擬器 介面規格

> 這份文件記錄本專案介面的**實際數值與行為**。改動畫面時先看這裡，改完回頭更新這裡。

## 1. 適用範圍與視窗尺寸

| 項目 | 值 |
| :--- | :--- |
| 畫面型態 | 單頁式網頁遊戲（行動端優先） |
| 預設尺寸 | `560px` × `100vh`（最大寬度 `560px`） |
| 最小尺寸 | 未定 |
| 可否縮放 | 可 |

## 2. 色票

**本專案為獨立專案，尚不屬於任何 series**（見 `docs/rules/INTERFACE_RULES.md` §2.1）。色票由本專案自行定義，完整記錄於下表，未採用 `docs/rules/tokens.css` 這份預設起點。

**自訂原因**：本專案需要一組能同時支撐深色與淺色四種佈景的完整 token 系統，`tokens.css` 的單一深色配色無法涵蓋。新架構的視覺識別為科技藍（主題 `a`）。

### 2.1 主題

**色票不是一組固定值，是四組整套替換的主題。** 主題由 `body[data-theme]` 決定，預設為 `a`。每個主題覆寫**全部**色票與圓角——包括 `--r`，所以圓角也是主題的一部分，不是固定值。

| 代碼 | 名稱 | 調性 |
| :--- | :--- | :--- |
| `a` | 科技藍 | **預設，新架構的視覺識別。** 深海軍藍底、電光藍強調，帶光暈 |
| `b` | 電子看板 | 近黑底、點陣字體、強光暈、圓角 4px |
| `c` | 報紙版面 | 米白底、襯線字體、無光暈、直角 |
| `d` | 現代儀表板 | 淺色底、無光暈、圓角 12px |

主題 `a` 原為「深綠記分板」（球場綠底、琥珀色強調），新架構改為科技藍。原始的深綠配色仍完整保存在 `index_legacy.html`，未來若要恢復可直接取用。`b`／`c`／`d` 三個主題維持原樣未動。

### 2.2 Token 定義

| Token | 主題 a（科技藍） | 主題 b | 主題 c | 主題 d | 用在哪 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `--bg` | `#070d16` | `#07090c` | `#f4efe1` | `#f6f7f6` | 頁面背景 |
| `--panel` | `#0e1826` | `#0c1014` | `#faf6ea` | `#ffffff` | 卡片與區塊底色 |
| `--panel2` | `#16243a` | `#10161c` | `#ece5d2` | `#f2f4f2` | 按鈕與次要區塊底色 |
| `--edge` | `#24384f` | `#232c36` | `#1e1c18` | `#e2e6e3` | 邊框線條 |
| `--text` | `#dce6f2` | `#d8dde2` | `#1e1c18` | `#1a201c` | 主要文字 |
| `--dim` | `#8298b4` | `#5c6873` | `#6f6857` | `#7a857e` | 次要文字、說明 |
| `--accent` | `#38bdf8` | `#ffb32e` | `#b3402f` | `#0f7b4d` | 強調、高光、球隊、正面數值 |
| `--good` | `#3ddc97` | `#49b675` | `#2f6b3a` | `#0f7b4d` | 正面狀態、成長 |
| `--bad` | `#f2616b` | `#e2695c` | `#b3402f` | `#c0392b` | 負面狀態、衰退 |
| `--info` | `#818cf8` | `#3a7bd5` | `#35506b` | `#4a76a8` | 資訊標記、連結 |
| `--gold` | `#f5c451` | `#ffb32e` | `#b3402f` | `#b8862d` | 重大事件卡的框線 |
| `--r` | `10px` | `4px` | `0px` | `12px` | 圓角 |
| `--btnbg` | `linear-gradient(#1b3a5c,#132b45)` | `#132018` | `#1e1c18` | `#0f7b4d` | 主要按鈕底 |
| `--btnedge` | `#2f5f8f` | `#49b675` | `#1e1c18` | `#0f7b4d` | 主要按鈕邊框 |
| `--btntx` | `#e8f2ff` | `#d8dde2` | `#f4efe1` | `#ffffff` | 主要按鈕文字 |
| `--shadow` | `rgba(0,0,0,.45)` | `rgba(0,0,0,.5)` | `rgba(30,28,24,.12)` | `rgba(26,32,28,.1)` | 陰影色 |
| `--glow` | `0 0 8px rgba(56,189,248,.4)` | `0 0 10px rgba(255,179,46,.6)` | `none` | `none` | 發光效果 |
| `--bgfx` | `radial-gradient(…rgba(30,90,160,.45)…)` | `none` | `none` | `none` | 場館光暈背景 |

**科技藍的配色邏輯**：`--accent` 是電光藍，用於強調與數值；`--info` 刻意選帶紫的靛藍而非另一種藍，才不會在滿版藍色調裡與 `--accent` 混淆。`--gold` 保留暖金色而非跟著轉藍——名人堂、重大事件這類「成就」時刻需要一個跳出整體色調的顏色才有份量。

### 2.3 相容別名

`--chalk` / `--amber` / `--blue` / `--clay` **不是獨立的色票**，而是指回真 token 的別名，只為了讓舊的行內樣板還能運作：

```css
--chalk: var(--text);
--amber: var(--accent);
--blue:  var(--info);
--clay:  var(--accent);
```

**新程式碼禁止使用這四個別名**，一律用 `--text` / `--accent` / `--info`。特別注意 `--clay` 指向的是琥珀色，不是土色——把它當獨立的橘褐色使用會得到錯誤的顏色。

## 3. 字體與字級

字體堆疊未採用 `tokens.css` 的預設起點，使用專案自訂堆疊。**其中 `--head` 與 `--disp` 隨主題變動**：

| Token | 主題 a | 主題 b | 主題 c | 主題 d |
| :--- | :--- | :--- | :--- | :--- |
| `--sans` | `'Noto Sans TC',-apple-system,'PingFang TC','Microsoft JhengHei',sans-serif`（四主題共用） | | | |
| `--mono` | `'IBM Plex Mono',ui-monospace,Menlo,monospace`（四主題共用） | | | |
| `--head` | `'Noto Sans TC',sans-serif` | `'DotGothic16',monospace` | `'Noto Serif TC',serif` | 同 a |
| `--disp` | `'IBM Plex Mono',monospace` | `'DotGothic16',monospace` | `'Noto Serif TC',serif` | 同 a |

`DotGothic16`、`IBM Plex Mono`、`Noto Sans TC`、`Noto Serif TC` 皆自 Google Fonts 載入，由 `client/index.html` 的 `<link>` 引入。**離線時會 fallback 成系統字體**，主題 b 與 c 的識別度會明顯下降——這是目前未解的問題，見 `DEVELOPER.md` §10。

| 用途 | 字級 | 字重 | 行距 | 字體 |
| :--- | ---: | :--- | ---: | :--- |
| 玩家名稱、計分板數值 | `17px` | `700` / `900` | 預設 | `sans` / `mono` |
| 內文 (`body`) | `15px` | `400` | `1.6` | `sans` |
| 卡片內文 | `14px` | `400` | `1.6` | `sans` |
| 屬性條、球隊名稱 | `12.5px` | `700` | 預設 | `mono` / `sans` |
| 卡片標題、標籤、表格 | `12px` | `700` / `400` | 預設 | `sans` / `mono` |
| 分隔線 | `11px` | `400` | 預設 | `sans` |
| 狀態燈號 | `10.5px` | `400` | 預設 | `sans` |
| 計分板標籤 | `10px` | `400` | 預設 | `sans` |

## 4. 間距

| 名稱 | 值 | 用於 |
| :--- | ---: | :--- |
| 視窗外緣 | `14px` | 內容距視窗四邊（上下左右一律相同） |
| 區塊間 | `10px` | 主要卡片與區塊之間 |
| 控制項間 | `8px` | 同一列的控制項之間、一般間距 |
| 標籤與欄位 | `6px` | 欄位標籤與其輸入框之間、小間距 |
| 微小間距 | `4px` | 元素內部的微小間距 |

- **四邊外緣必須相等**，不得單獨加大某一邊。
- 不得使用表外的值；真的需要就先改這張表。

## 5. 控制項

### 尺寸

| 項目 | 值 |
| :--- | :--- |
| 標準高度 | `44px`（按鈕，基於 `11px` 上下內邊距與 `15px` 字級加總預估） |
| 緊湊高度 | `28px`（未特別指定，基於預設推估） |
| 圓角 | `var(--r)`——**隨主題變動**（a `10px` / b `4px` / c `0px` / d `12px`），見 §2.2。小元件用 `min(var(--r), 4px)` 或 `min(var(--r), 6px)` 收斂 |
| 水平內邊距 | `14px`（`.btn`） |

同一列的控制項**必須等高、字級相同**，寬度可不同。

### 按鈕角色

| 角色 | 外觀 | 用於 |
| :--- | :--- | :--- |
| 主要 | `.btn.main` 漸層底 `#2a5038` 至 `#1f3f2c`，置中對齊 | 主要動作，如「開始生涯」 |
| 次要 | `.btn` 實心，`--panel2` 底，左對齊 | 遊戲內一般選項 |
| 警告 | `.btn.warn` 邊框為 `#6a3c3c` | 危險或警告動作 |

### 按鈕狀態

| 狀態 | 表現 |
| :--- | :--- |
| hover | 邊框變色為 `--amber` (`#ffc95c`) |
| active | 縮放 `transform: scale(0.98)` |
| disabled | 未使用 |
| 進行中 | 未使用 |

## 6. 視覺效果

### 不透明度

未沿用共用 token。

| 檔位 | 值 | 用於 |
| :--- | ---: | :--- |
| 提示 | `55%` | 版本號不透明度 |
| 停用 | `45%` | 已達上限狀態 (`.capped`) |
| 骰子使用 | `22%` | 骰子已使用狀態 (`.die.used`) |

### 光暈 / 模糊 / 漸層

| 效果 | 是否使用 | 用途與強度 |
| :--- | :--- | :--- |
| 光暈（box-shadow 發光） | 使用 | 計分板陰影 `0 6px 18px #0009`、燈號發光 `0 0 8px var(--amber)` |
| 高斯模糊（backdrop-filter） | 使用 | 底部操作區背景模糊 `blur(6px)` |
| 漸層 | 使用 | 頁面背景 `radial-gradient`、計分板背景 `linear-gradient` |
| 文字陰影（text-shadow） | 使用 | 計分板數值發光 `0 0 8px #ffc95c55` |

## 7. 狀態與回饋

| 狀態 | 表現 |
| :--- | :--- |
| 焦點 | 未使用（無明確 `:focus-visible` 樣式） |
| hover | 按鈕邊框變為 `--amber`，`.yr-head` 文字變為 `--amber` |
| 停用 | 未使用 |
| 載入 / 進行中 | 未使用 |
| 錯誤 | 使用 `.btn.warn` 或 `--bad` 色彩表現 |

**未採用預設起點的焦點樣式與停用表現**：本專案為獨立專案（見 §2），視覺以球場綠為核心，與 `tokens.css` 的中性暗色系有明確區隔需求。

> **已知不合規**：`INTERFACE_RULES.md` §4.5 規定所有可用 Tab 走到的元件**必須**有可見的焦點樣式，不得為「未使用」。上表的「焦點」一列目前不合規。舊版 `index_legacy.html` 為唯讀保留、不再修改，此項於新架構的介面實作時補上。

### 日誌 / 訊息等級（若有）

未採用 `tokens.css` 的 `--token-color-level-*` 等級配色。舊版用的是**卡片色調**，不是日誌等級：

| 等級 | 顏色 |
| :--- | :--- |
| `good` | `--good` (`#8fd08f`) |
| `bad` | `--bad` (`#e2695c`) |
| `info` | `--blue` (`#7fb3d5`) |
| `gold` | `--amber` (`#ffc95c`) |

> **待新架構補上**：`INTERFACE_RULES.md` §4.6 規定日誌／訊息等級必須使用 `DEBUG`／`INFO`／`WARN`／`ERROR` 四個識別字串，且**禁止**只靠顏色傳達等級。舊版只有上述卡片色調、沒有等級字串。舊版 `index_legacy.html` 唯讀保留、不再修改，此項於新架構的介面實作時建立。

## 8. 畫面結構

桌面（>920px）為左右兩欄。920px 以下**不是把兩欄堆成一欄**，見 8.1。

```text
+---------------------+---------------------------------+
| #board              | #panel-stats                    |
| 姓名·守位·投打      | 最近一季的成績表                 |
| 年份/年齡/綜合/點數 |                                 |
| 年薪·SEED·榮譽      |                                 |
| [特性][特性]        |                                 |
+---------------------+---------------------------------+
| #panel-abilities    | #panel-log (Scrollable)         |
|                     |                                 |
| 體力                | [ 卡片 ]                        |
|  [====|--] 42/58    | [ 卡片 ]                        |
| 投手                | [ 卡片 ]                        |
|  [==|----] 31/55    |   ↑ 連續不清空                  |
| 野手                |                                 |
|  [======|] 55/55    +---------------------------------+
|                     | #panel-act (Sticky Bottom)      |
|  ↑ 加點時可直接點擊 | 骰面 / 選項按鈕                 |
+---------------------+---------------------------------+
```

| 區塊 | 元件 | 職責 |
| :--- | :--- | :--- |
| `#board` | `ui/game/Board.tsx` | 球員身分：姓名、守位、投打慣用手、所屬、記分板、SEED。底下還掛一份**只有手機看得到**的特性列（`#bd-traits`） |
| `#panel-abilities` | `ui/game/AbilityPanel.tsx` | 三組能力（體力／投手／野手）。**加點時能力列本身就是按鈕**，不另開選單 |
| `#panel-stats` | `ui/stats/StatsPanel.tsx` | 最近一季的成績表與狀態（特性），常駐右上。**手機整塊不出現**（見 8.1） |
| `#panel-log` | `ui/game/EventLog.tsx` | 事件卡片歷程，可捲動，**跨年度連續不清空**；新內容出現時一律捲到底 |
| `#panel-act` | `ui/game/GameScreen.tsx` | 骰面與選項按鈕，常駐右下 |
| `#start` | `ui/start/StartScreen.tsx` | 開局設定畫面（全螢幕覆蓋） |

生涯結束後 `#panel-act` 多一顆「下載生涯成績」，在「重新開局」上面，而且**它才是
主要按鈕**（`.btn.main`，置中）——生涯剛結束的那一刻，把這一段留下來比開下一局
更接近玩家想做的事。它把這段生涯
畫成一張 PNG：**球員卡 → 狀態 → 引退之日 → 生涯年表 → 榮譽**，順序與畫面不同
（畫面上引退之日是事件流裡的一張卡，排在年表之前；圖是要傳出去給人看的，先講
他是誰、再講那一天發生了什麼事，最後才攤開數字）。

圖是自己畫在 `<canvas>` 上的（`ui/card/careerImage.ts`），不是把卡片截圖：內容順序本來
就不一樣，截圖套件仍得先兜一份隱藏 DOM，換來的只是多一個依賴與一堆它畫不出來
的新 CSS。配色與字體讀 CSS 變數的計算值，**跟著玩家當下的主題走**。圖上每一格
的內容都由 `careerCardOf()` 走畫面上同一批函式組出來——成績的算法只該有一份。

圖的左下角落款寫「棒球生涯模擬器」與 `https://yakyulife.overmind.men`：圖會被傳
到看不見這個遊戲的地方，那時它是唯一的來源說明。

**狀態（特性）兩個版面掛在不同地方，但那是同一個 `TraitList` 元件**——內容不會
分岔，分岔的只有掛在哪裡：

- **桌面**接在 `#panel-stats` 的成績表下面。成績與狀態是同一段時間的兩種側寫。
- **手機**釘在 `#board` 底下（`#bd-traits`，不帶「狀態」小標）。手機把成績面板
  整塊收掉，狀態得有地方去，而記分板是釘在畫面頂端、兩頁都看得到的那一塊。

**成績表在手機上不出現**：每季結算的事件卡已經印過同一份數字（含 OPS+／ERA+），
常駐面板是講第二次，而它佔掉的正是手機最貴的那一屏。

### 8.1 手機（≤920px）：能力頁 ‖ 事件頁

手機不堆疊，而是把中間那一段做成**兩頁的橫向走馬燈**。記分板與動作區釘在上下
兩端不動，中間一次只看一頁。

```text
+---------------------------------+
| #board（fixed）                 |
+---------------------------------+
| ‹        [ 一頁 ]           ›   |   ← 半透明箭頭，到底的那一側收起
|                                 |
|   能力頁          事件頁        |
|   #col-left  ‖   #col-right     |
|                                 |
|            ● ○                  |   ← 頁點
+---------------------------------+
| #panel-act（fixed）             |
+---------------------------------+
```

| 規則 | 內容 |
| :--- | :--- |
| 兩頁 | **能力頁** = `#col-left`（記分板以外的部分，即 `#panel-abilities`）；**事件頁** = `#col-right`（`#panel-log`） |
| 切換 | 左右滑動（scroll-snap）或點半透明箭頭，兩者是同一個機制 |
| 自動切頁 | **只在轉折的那一刻切一次**：需要加點時切到能力頁，加完切回事件頁。加點的過程中不再干預——玩家自己滑去看卡片是他的選擇 |
| 先讀再切 | 切到能力頁**慢一秒**（`READ_FIRST`）。卡片剛跳出來就換頁的話，玩家會在點完能力之後才知道發生了什麼事 |
| 每按一次對一次帳 | 動作區的按鈕按下去之後重新確認該待在哪一頁：沒有點數也沒有骰子要點就回事件頁。轉折只認得到變化，認不出「本來就不需要、但畫面停在能力頁」這種已經歪掉的狀態 |
| 起始頁 | 事件頁。故事線是常態，能力頁是被叫出來的那一頁。但開局第一個提問就是配點時直接停在能力頁——那個「轉折」不存在，沒有人會把他帶過去 |
| 手指還按著 | **自動切頁要等玩家放開手才滑。** 能力列有長按連發，點數點完的那一刻手指多半還壓著，而瀏覽器一偵測到觸控就會取消進行中的 smooth scroll，畫面會停在兩頁中間。**等待有上限（1.2 秒）**——`pointerup` 不保證送得到，無限等的話那一筆待辦永遠不會兌現，看起來就是「偶發不會自動滑回去」 |
| 收尾 | 滑完若仍卡在兩頁之間，瞬移對齊（不是再滑一次——會走到這裡就是因為 smooth 被打斷過）。**已經停在另一頁就不動**：那是玩家自己滑過去的 |

走馬燈**不另外包一層 DOM**，是 `#game` 自己在這個斷點下變成橫向 snap 容器，
`#col-left` / `#col-right` 各佔一頁。理由：那兩塊在桌面分屬左右欄，一個 DOM 生
不出兩種父子關係；硬包一層就得改寫桌面的 grid，而 grid 的列高會把 `#board` 與
`#panel-stats` 綁進同一列，能力欄的起點就跟著成績表的高度跑。換成讓 `#game` 換
角色之後，**手機的規則全部關在 `@media (max-width:920px)` 裡，桌面那段 CSS 一行
都不必碰**。

代價寫在這裡免得日後有人拿掉：`#board` 與 `#panel-act` 在手機是 `position:fixed`
（留在流裡會跟著頁一起滑走），因此中間那條帶子的上下界要靠 `--board-h` /
`--act-h` 兩個 CSS 變數，由 `ResizeObserver` 量出來寫進 `:root`——記分板的高度隨
合約、年薪、特性數量變動，動作區隨選項數量變動，兩個都不是常數。

### 浮動視窗

兩張，共用 `.modal`，寬度不同：

| 視窗 | 寬度 | 內容 |
| :--- | :--- | :--- |
| 帳號（登入／註冊） | `620px` | 一排輸入欄。**刻意維持窄的**——輸入欄拉寬只會變成一條 |
| 成就 | `1200px`（`.modal.wide`） | 四個分頁：成就／天賦／天梯／更新 |

成就視窗的**內容排兩欄**，分頁列與天梯的範圍列不分欄（它們是選單，跟著視窗寬度走）。
兩欄的做法依內容而異：

| 分頁 | 兩欄怎麼切 | 為什麼 |
| :--- | :--- | :--- |
| 成就 | `column-count` 切在**分類**上（中職／日職／生涯…） | 多欄的閱讀順序是「左欄由上往下、再跳右欄」，保得住固定排序（ADR 0031）；格線會把相鄰兩組並排成一列，順序就散了 |
| 天賦 | 同上，切在**分組**上（成長／衰退／事件卡…） | 同上。「成長」那六張卡不會被切成左三右三 |
| 天梯 | 格線切在**側別**上：野手左、投手右，每側內部再 auto-fill 兩欄 | **語意分欄，不是平衡分欄**。野手 18 塊榜、投手 13 塊，兩側高度本來就不齊；換到的是「不必滑過整個野手才看得到投手」 |
| 更新 | `column-count` 切在**條目**上，版本仍是一塊一塊往下疊 | 一個版本有上百條，把版本當成不可切開的區塊等於要求瀏覽器把一根很高的柱子塞進半欄——它塞不下，只會讓左欄爆出去、右欄全空 |

### 榮譽的標籤

四組標籤（獎項／里程碑／業餘與國際賽／人生）**長得一模一樣**。這裡曾經用虛線、
點線與不同的透明度區分來源，但每一組上面本來就寫著自己的小標——同一件事編碼
兩次，而形狀那一份還要先看得懂才讀得出來。淡掉的那兩組更糟：透明度在版面上的
意思是「比較不重要」，可是一面國際賽的金牌並不比一座打點王次要。

不論哪一種，**被分欄的單位本身不能被切開**（`break-inside: avoid`）：一個聯盟、一組
天賦、一條變更橫跨兩欄，讀的人要來回掃兩次。

1200px 的來源是天梯反推的：野手側要放得下兩塊 `260px` 的榜（`260×2 + 18 = 538`），
投手側同寬，中間一個 `18` 的溝，加上左右內距 `36`，共 `1130`。

`920px` 以下三個分頁一律退回單欄，與主畫面同一個斷點。

### 能力列的量表

能力條的刻度是**頭 20、尾 80**——20 是球探量表的下限，畫成從 0 開始會讓所有
球員看起來都很強。只有被事件提升過上限的能力，尾端才會延伸到 80 以上（每項
最多 +5，故最高 85）。

`<i>` 是目前值，`<em>` 是潛力天花板的刻度線。

### 骰面

季初訓練的骰子用 `#dice` / `.die` 呈現：已分配的變暗（`.used`）、目前這顆高亮
（`.active`）、6 點另以顏色標示（`.six`）。分配完最後一顆即收起——後續的提問
與骰子無關。
